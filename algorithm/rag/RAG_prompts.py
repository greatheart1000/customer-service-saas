#coding:utf-8

"""1. 提取实体的 Prompt
优化目标是支持细粒度实体类型划分，明确实体类型的范围，同时允许扩展更多类型。"""

extracted_entities_prompt = """
请从以下文本中提取所有实体（人名、地点、组织、技术、功能等），并为每个实体指定具体的类型（如：系统、设备、技术、功能等）
其次，合理设计实体类型的层次和粒度，"具体类型"用更细粒度的词语区分：
\n\n[text]\n\n返回的格式为：
[['实体1', '具体类型'], ['实体2', '具体类型'], ...]
"""

"""2. 总结实体的 Prompt
优化目标是合并相似实体，确保实体清晰且信息完整，同时保持细粒度类型分类。"""

summarized_entities_prompt = """
请对以下实体进行总结，合并相似的实体，并确保信息的完整性和清晰性。请在总结中包含每个实体的主要功能或特点：
\n\n[entities]\n\n返回的格式为：
[['总结实体1', '类型', '功能或特点'], ['总结实体2', '类型', '功能或特点'], ...]
"""

"""3. 构建实体关系的 Prompt
优化目标是提取更细粒度的实体关系，并明确关系的类型和方向。"""

create_base_entity_graph_prompt = """
根据以下实体，构建一个基础实体图，描述实体之间的具体关系和交互。请使用明确的关系类型（如：依赖、使用、关联等）,并且实体关系类型具有多样性，不死板：
\n\n[entities]\n\n返回的格式为：
[['实体1', '关系类型', '实体2'], ['实体3', '关系类型', '实体4'], ...]
"""

"""4. 去重和清洗实体的 Prompt
优化目标是确保最终实体集合不冗余且清晰，避免重复或模糊的实体。"""
create_final_entities_prompt = """
请对以下实体进行去重和清洗，确保每个实体清晰且不冗余，避免使用模糊的术语。返回最终的实体集合：
\n\n[entities]\n\n返回的格式为：
[['最终实体1', '类型'], ['最终实体2', '类型'], ...]
"""

"""5. 生成图节点的 Prompt
优化目标是生成描述独立且唯一的图节点，确保每个节点有唯一标识符。"""

create_final_nodes_prompt = """
根据以下实体生成图节点的描述，确保每个节点都有唯一标识符，返回的格式为：
\n\n[entities]\n\n返回的格式为：
[['节点ID', '实体名'], ['节点ID', '实体名'], ...]
"""

"""6. 生成知识图谱的 Prompt
优化目标是结合实体、关系、节点信息生成完整的知识图谱，明确节点和边的特性。"""

generate_knowledge_graph_prompt = """
根据以下信息生成知识图谱：

1. 实体之间的关系：
[relationships]

2. 总结实体及其类型：
[summarized_entities]

3. 图节点信息：
[final_nodes]

请结合这些信息，详细描述如何构建知识图谱，包括节点、边及其特性，并给出图的整体结构示例。返回的格式应包括节点和边的详细信息，例如：
{
    "nodes": [["节点ID", "实体名", "类型", "详细描述"], ...],
    "edges": [["实体1节点ID", "实体2节点ID", "关系类型"], ...]
}
"""

summarize_and_extract_keywords_prompt = """
请针对以下文档片段进行操作：生成简洁的摘要，并提取关键词。摘要需精准提炼出主要观点和关键信息，关键词要能够有效地代表文档的主题与内容。
给定文档:\n[text]\n返回结果的格式如下：
{
    "summary": "文档摘要",
    "keywords": [["关键词1"], ["关键词2"], ...]
}
文档的摘要不超过200字，关键词长度也必须不超过8位
"""